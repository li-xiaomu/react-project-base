// 切换模式： createBrowserRouter createHashRouter
import { createBrowserRouter } from 'react-router-dom';
import { Route } from './route';
const router = createBrowserRouter(Route);

export default router;
